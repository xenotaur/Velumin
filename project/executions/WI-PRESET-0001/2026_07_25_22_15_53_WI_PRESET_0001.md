---
execution_id: 2026_07_25_22_15_53_WI_PRESET_0001
prompt_id: PROMPT(WI-PRESET-0001:WI_PRESET_0001)[2026-07-25T22:05:36-04:00]
work_item: WI-PRESET-0001
status: landed
rerun_of: 
pr: https://github.com/xenotaur/Velumin/pull/14
commit: d4e5f9b18807ad7dfb2f582dfac1b2e1ca6d0975
created_at: 2026-07-25T22:15:53-04:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-PRESET-0001.md
session_transcript: claude-app:10b17519-5c53-4a2a-be87-5686ece435f9
---

# Summary

Implement WI-PRESET-0001: the v1 public display-preset API adopted in DP-0007.
Prior-art check: no existing public preset selector; `VectorDisplayPreset` was
internal (`#[allow(dead_code)]`), used only by the tester default and unit tests.

# Result

- `webgpu_vector_lib/src/lib.rs`: `VectorDisplayPreset` is public,
  `#[non_exhaustive]`, `Clone/Copy/Debug/PartialEq/Eq`, and a wasm-bindgen enum on
  wasm32 (plain enum on host). Threaded a `preset` param through `Renderer::new`;
  added `create_with_preset` (factory; `create` keeps defaulting to
  `ArcadeBalanced`) and runtime `set_display_preset`, both wasm-bindgen exported
  (`createWithPreset` / `setDisplayPreset`).
- `webgpu_vector_lib/web/index.html`: a display-preset dropdown (baseline +
  Blasterites) and a `?preset=<name>` query param (via `createWithPreset`).
- `webgpu_vector_lib/web/smoke.mjs` + `smoke/reference/`: capture the three
  non-default presets on the deterministic pre-impact frame; committed grid
  signatures and human PNGs.
- `README.md`: documented the public preset API.

The plan-gate risk (`#[wasm_bindgen]` + `#[non_exhaustive]`) was verified to
compile cleanly on wasm32 before building the rest. Presets render visibly
distinct (bright fraction 1.55% / 3.10% / 1.92% for MonochromeBeam /
ColorQuadraScan / CleanNeon vs 1.03% for the ArcadeBalanced default), verified in
a WebGPU-capable browser. Per DP-0007 v1, no public custom-settings API was added
and `render_blasterites_tuner` stays internal.

CHAIN-NOTE: cycles=1; stops=0; gates=[plan,merge]; friction="review reconciliation: lifecycle-in-impl-PR vs closeout convention, plus a real preset-selection smoke gap the grid check missed"; note="first code WI in the preset line; #[wasm_bindgen]+#[non_exhaustive] risk cleared at the plan gate"

# Validation

- `scripts/version` — Rust 1.87.0, wasm-pack 0.13.1, Node 20.20.0.
- `scripts/format --check` — clean.
- `scripts/lint` — clippy clean (`-D warnings`, wasm target).
- `scripts/test` — 14 passed.
- `lrh validate` — 0 errors, 0 warnings.
- `scripts/smoke` — 9/9 (baseline, tester 4:3/wide/tall, tuner, + 3 presets);
  each preset matches its committed reference (MAD 0.000).

# Follow-up

- On merge, `/lrh-closeout` moves WI-PRESET-0001 to resolved.
- A public custom-settings API remains a deferred DP-0007 follow-up (not in v1).
- Update `session_transcript` from `pending` to `claude-app:<session-id>` after
  the session ends.
