---
execution_id: 2026_07_25_02_01_17_WI_SMOKE_0001
prompt_id: PROMPT(WI-SMOKE-0001:WI_SMOKE_0001)[2026-07-25T01:38:42-04:00]
work_item: WI-SMOKE-0001
status: landed
rerun_of: 
pr: https://github.com/xenotaur/Velumin/pull/11
commit: 9c0f1bae00ef5572462d0b2a681303c37ff928bb
created_at: 2026-07-25T02:01:17-04:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-SMOKE-0001.md
session_transcript: claude-app:10b17519-5c53-4a2a-be87-5686ece435f9
---

# Summary

Implement WI-SMOKE-0001: an automatable screenshot smoke check for the demo
scenes, building on the manual EV-0009 capture, plus a GPU-free Rust
determinism guard. Prior-art check: no existing screenshot/smoke infrastructure
(scripts/README.md noted browser visual smoke was not yet in the script lane);
the placeholder `test:visual-smoke` npm script only built. Readiness: the work
item was flagged not prompt-ready (missing Required Changes / Validation
sections), which this change fills in.

# Result

Delivered the Hybrid scope confirmed at the plan gate:

- Freeze-frame harness mode in `webgpu_vector_lib/web/index.html` (`?frame` /
  `?frame=<ms>` / `?t=<ms>`), signalling `window.__smokeReady` / `__smokeError`;
  existing behavior preserved when `?frame` is absent.
- Rust determinism guard in `webgpu_vector_lib/src/lib.rs`
  (`blasterites_tester_is_deterministic_and_animates`) — the always-on, GPU-free
  regression check.
- `webgpu_vector_lib/web/smoke.mjs` (Playwright + pngjs) + `scripts/smoke`:
  captures the baseline / blasterites (pre & post) / tuner scenes at 4:3, wide,
  and tall viewports and asserts structural properties; skips cleanly when no
  WebGPU adapter is present. Kept out of `scripts/validate`.
- devDeps (playwright, pngjs) + `smoke` npm script; committed wide/tall
  reference frames; gitignored `smoke-out/`; documented in README and
  scripts/README.

Two headless gotchas were found and fixed during implementation: pre-layout
`WebGPU.create()` sizing the surface 1x1 (fixed by awaiting layout in frame
mode), and in-page `drawImage` of a WebGPU canvas reading back black in headless
Chromium (fixed by analyzing the Playwright compositor screenshot via pngjs).
Headless WebGPU turned out to work in this Playwright Chromium, so the capture
happy-path was verified here, not merely documented.

CHAIN-NOTE: cycles=1; stops=0; gates=[plan,merge]; friction="headless-WebGPU readback quirks (pre-layout 1x1 sizing, black in-page drawImage) needed workarounds"; note="hybrid scope confirmed at plan gate; capture happy-path verified live, not merely documented"

# Validation

- `scripts/version` — Rust 1.87.0, wasm-pack 0.13.1, Node 20.20.0, Vite 6.2.2.
- `scripts/format --check` — clean.
- `scripts/lint` — clippy clean (`-D warnings`, wasm target).
- `scripts/test` — 14 passed, incl. `blasterites_tester_is_deterministic_and_animates`.
- `lrh validate` — 0 errors, 0 warnings.
- `scripts/smoke` — 6/6 checks passed on a WebGPU-capable Chromium (baseline,
  blasterites-4x3-pre/post, wide, tall, tuner); pre-impact bright fraction
  differed from post-impact, exercising the static-render guard.

# Follow-up

- On merge, `/lrh-closeout` moves WI-SMOKE-0001 to resolved.
- Optionally wire `scripts/smoke` into a GPU-enabled CI job later; today it is an
  explicit opt-in check.
- Remaining DP-0006 follow-ups unchanged: public-preset-API decision and visual
  capture of the non-default presets.
- Update `session_transcript` from `pending` to `claude-app:<session-id>` after
  the session ends.
