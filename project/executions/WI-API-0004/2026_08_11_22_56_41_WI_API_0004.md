---
execution_id: 2026_08_11_22_56_41_WI_API_0004
prompt_id: PROMPT(WI-API-0004:WI_API_0004)[2026-08-11T22:34:21+00:00]
work_item: WI-API-0004
status: landed
rerun_of:
pr: https://github.com/xenotaur/Velumin/pull/33
commit: 7f8cedc63af79292682b10042918e5b8b47e57f7
created_at: 2026-08-11T22:56:41+00:00
agent: codex_app
instruction_source: project/work_items/proposed/WI-API-0004.md
session_transcript: codex-app:019fc56e-17bf-78f0-9796-8a754d713247
---

# Summary

Implemented WI-API-0004's DP-0008 browser frame view mapping extension. The work exposes a small `VectorFrameView` public API for default centered 4:3, explicit logical extents, and browser-style canvas-pixel coordinates while preserving the existing demo entrypoints and default rendering behavior.

# Result

- Added `VectorFrameView` and routed `WebGPU::render_frame`, `WebGPU::render_frame_with_view`, `WebGPU::render_commands`, and `WebGPU::render_commands_with_view` through the same view-aware renderer path.
- Kept built-in baseline, Blasterites tester, and tuner routes on the existing centered 4:3 default mapping.
- Updated the public `?demo=frame-api` harness to submit Replication Vector / Blasterites-style geometry in canvas-pixel coordinates through `VectorFrameView.canvasPixels`.
- Added host tests proving invalid views are rejected, the default centered 4:3 viewport is preserved, canvas-pixel coordinates map into the full viewport, `VectorFrame` and typed `&[VectorCommand]` use matching mapped geometry, and pixel stroke widths remain stable across horizontal and vertical lines.
- Updated README coverage for the default coordinate model, explicit mapping options, the JS `VectorFrame` boundary, the Rust/WASM typed command-slice boundary, and the boundary between renderer mapping and game-owned simulation policy.
- Fresh independent self-review found two issues before PR push: mapped pixel stroke widths were aspect-dependent, and the README pixel-view example was misleading. Both were fixed before opening PR #33.

# Validation

- `scripts/version tools` — rustc 1.87.0, cargo 1.87.0, rustfmt 1.8.0, clippy 0.1.87, wasm-pack 0.13.1, node v20.20.0, npm 10.8.2, vite 6.2.2.
- `scripts/format --check --diff` — passed.
- `scripts/lint` — passed.
- `scripts/test` — passed: 2 `velumin_core` tests, 27 `webgpu_vector_lib` tests, and doc tests.
- `scripts/baseline` — passed: cargo check, wasm-pack build, and Vite production build.
- `lrh validate` — passed: 0 errors, 0 warnings.
- `scripts/smoke` — passed 11/11 checks, including `frame-api-4x3` and `frame-api-wide-pixels` with reference MAD 0.000; captures written to `webgpu_vector_lib/web/smoke-out`.

# Follow-up

- Wait for the automatic initial PR review/checks on PR #33.
- If actionable comments arrive, address them with `/lrh-review-response` or self-review as appropriate without requesting extra GitHub review-agent retriggers.
- After merge, run `/lrh-closeout https://github.com/xenotaur/Velumin/pull/33` to mark this execution landed and resolve `WI-API-0004`.
