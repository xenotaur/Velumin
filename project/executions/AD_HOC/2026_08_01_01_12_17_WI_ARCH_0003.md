---
execution_id: 2026_08_01_01_12_17_WI_ARCH_0003
prompt_id: PROMPT(AD_HOC:WI_ARCH_0003)[2026-08-01T01:10:49+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/Velumin/pull/21
commit: 
created_at: 2026-08-01T01:12:17+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-ARCH-0003.md
session_transcript: claude-app:10b17519-5c53-4a2a-be87-5686ece435f9
---

# Summary

Creates WI-ARCH-0003: scopes the narrow, implementable-now residue of
DP-0002 Phase 2's "explicit adapter/capability negotiation for web and
desktop" bullet, following `WI-ARCH-0002` (PR #20).

# Result

Before drafting, surfaced a scoping tension to the user: `WI-ARCH-0002`
explicitly listed desktop-specific capability negotiation as a Non-Goal
since no native host exists yet, so a WI literally titled "desktop-side
adapter/capability negotiation" risked being unimplementable as scoped.
Investigated the current code and found the negotiation logic itself
(format/alpha-mode/present-mode/limits checks in `Renderer::new`) is
already platform-neutral post-`WI-ARCH-0002` — it applies to any future
native surface with no changes needed. Presented this finding to the user
with two options (scope a narrower implementable slice, or defer
everything); user chose to scope the narrow slice: turning
`RendererError` from a bare `String` wrapper into a typed enum, so a
future native caller could match on which specific negotiation check
failed.

Wrote `project/work_items/proposed/WI-ARCH-0003.md`. Prior-art check:
`WI-ARCH-0002` introduced `RendererError` as a plain `String` wrapper and
explicitly scoped out desktop-specific negotiation as deferred; this item
is the narrow residue of that deferral, not a duplicate.

Opened as PR #21 (branch `xenotaur/feat/wi-arch-0003`).

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- No code lanes: this is a `project/work_items/` markdown planning
  artifact only; no implementation in this PR.

# Follow-up

- Implement WI-ARCH-0003 via `/lrh-implement` in a subsequent run, if the
  user chooses to pursue it (its own Risk Notes flag that its value is
  speculative until a native caller exists to consume the typed variants).
