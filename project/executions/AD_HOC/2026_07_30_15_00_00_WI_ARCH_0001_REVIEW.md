---
execution_id: 2026_07_30_15_00_00_WI_ARCH_0001_REVIEW
prompt_id: PROMPT(AD_HOC:WI_ARCH_0001_REVIEW)[2026-07-30T14:56:26-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_07_30_14_43_31_WI_ARCH_0001
pr: https://github.com/xenotaur/Velumin/pull/16
commit: 
created_at: 2026-07-30T15:00:00-04:00
agent: claude_app
instruction_source: https://github.com/xenotaur/Velumin/pull/16
session_transcript: claude-app:10b17519-5c53-4a2a-be87-5686ece435f9
---

# Summary

Review response for PR #16 (WI-ARCH-0001 creation). Addressed four codex
threads. Linked to the primary WI-creation record via `rerun_of`.

# Result

All four threads triaged present, valid, in-scope; fixed, none skipped:

- codex `r3685389522` (P1, unsupported crate-name claim): the WI said a
  "maintainer decision" settled the crate-rename question, but DP-0002 still
  says "undecided" and `design.md` still carries its TODO, with no decision-
  log entry. Added `project/memory/decision_log.md` entry
  "2026-07-30: Scope WI-ARCH-0001 without the webgpu_vector_lib rename",
  recording the actual, narrower decision, and reworded the WI to state this
  decides only this item's scope, leaving DP-0002's broader question open.
- codex `r3685389527` (missing scripts/baseline): `scripts/smoke` runs
  wasm-pack + a Vite dev server, not the wasm-target `cargo check` + Vite
  production build that `scripts/baseline` performs (which the WI's own Risk
  Notes already required). Added it to Validation.
- codex `r3685389532` (smoke SKIP loophole): on GPU-less environments
  `smoke.mjs` prints SKIP and exits 0, which could satisfy "scripts/smoke
  passes" without ever comparing a capture to the committed references.
  Reworded the acceptance criterion to require actual per-scene captures
  matching the reference signatures, explicitly rejecting a SKIP exit.
- codex `r3685389542` (unused bytemuck dependency): none of the extraction
  candidates (`Vec2`, `Color`, `StrokeStyle`, `Line`, `Polyline`,
  `VectorCommand`, the preset/settings types, `RenderViewport`) derive
  `bytemuck::Pod`/`Zeroable` — verified against the current `lib.rs`; only
  `Vertex`/`GlowVertex` (staying in `webgpu_vector_lib`) use it. Removed
  `bytemuck` from `velumin-core`'s required dependency.

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- No code lanes: this change is `project/work_items/` and
  `project/memory/decision_log.md` markdown only.

# Follow-up

- None; proceed to confirm-fixes.
