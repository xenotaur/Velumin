---
execution_id: 2026_07_25_22_31_34_WI_PRESET_0001_REVIEW
prompt_id: PROMPT(AD_HOC:WI_PRESET_0001_REVIEW)[2026-07-25T22:23:27-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_07_25_22_15_53_WI_PRESET_0001
pr: https://github.com/xenotaur/Velumin/pull/14
commit: 
created_at: 2026-07-25T22:31:34-04:00
agent: claude_app
instruction_source: https://github.com/xenotaur/Velumin/pull/14
session_transcript: pending
---

# Summary

Review response for PR #14 (WI-PRESET-0001). Addressed two codex threads.
Linked to the primary implement record via `rerun_of`.

# Result

Both threads present, valid, in-scope; fixed, none skipped:

- codex `r3651543395`: the three preset smoke checks did not actually validate
  preset *selection* — the normalized per-frame grid reference can't tell the
  presets apart (they share geometry; MAD 0.0007-0.0017 vs a 0.16 tolerance), so
  a regression rendering ArcadeBalanced everywhere would still pass. Added a
  color-aware lit-pixel fingerprint (brightFraction + mean lit RGB) to
  `smoke.mjs` and a pairwise cross-preset distinctness assertion (ArcadeBalanced
  default vs each non-default preset, same run/adapter); distances are well above
  the 0.08 threshold and collapse to ~0 on a selector regression.
- codex `r3651543392`: the control-plane lifecycle lagged the code. Marked
  DP-0007 `implementation_status: implemented` (implemented_by WI-PRESET-0001) and
  reconciled design/status/roadmap/focus/context and the DP-0006 follow-up to read
  the public preset API as implemented and the presets as captured. Per LRH flow,
  WI-PRESET-0001 stays `proposed` until its `/lrh-closeout` resolves it with the
  merge commit (the one genuinely closeout-gated step); next actions now point at
  DP-0007's deferred custom-settings API and the next workstream.

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- `scripts/smoke` — 9/9, now including the cross-preset distinctness guard.
- `scripts/format --check` — clean (no Rust changes this round; smoke.mjs is JS).

# Follow-up

- Update `session_transcript` from `pending` to `claude-app:<session-id>` after
  the session ends.
