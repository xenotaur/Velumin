---
id: EV-0002
title: Browser Rendering Baseline Command Verification
status: active
date: 2026-05-08
work_item: WI-RENDER-0001
---

# EV-0002: Browser Rendering Baseline Command Verification

## Summary
- Verified the current Rust/WASM/Vite baseline commands before beginning the WebGPU modernization sequence.
- Added npm scripts so future contributors and agents can rerun the baseline from `webgpu_vector_lib/web/`.
- Verified the current browser smoke output in the Codex in-app browser against the local Vite server.

## Commands Verified
- From `webgpu_vector_lib/`: `cargo check --target wasm32-unknown-unknown`
- From `webgpu_vector_lib/`: `wasm-pack build --target web`
- From `webgpu_vector_lib/web/`: `npx vite build`

## Result
- `cargo check --target wasm32-unknown-unknown` passed.
- `wasm-pack build --target web` passed and produced `webgpu_vector_lib/pkg`.
- `npx vite build` passed and produced `webgpu_vector_lib/web/dist`.
- `npm run baseline` passed when allowed to run outside the sandbox so `wasm-pack` could complete its `wasm-bindgen` and `wasm-opt` steps.
- Browser smoke output showed a white horizontal line on a black canvas.
- Browser console reached setup, surface creation, adapter acquisition, device creation, surface configuration, shader creation, pipeline creation, render call, and frame presentation.

## Browser Evidence
- URL: `http://127.0.0.1:5173/`
- Browser title: `WebGPU Vector Graphics`
- Adapter log: `ANGLE (Apple, ANGLE Metal Renderer: Apple M1 Max, Unspecified Version) (Gl)`
- Visual result: visible white line on black background; not all-black and not all-white.

## Remaining Follow-up
- Future renderer changes should repeat this check and preserve or intentionally update this visual baseline.
