#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
MANIFEST="$ROOT/Cargo.toml"

cargo clippy --manifest-path "$MANIFEST" --target wasm32-unknown-unknown --all-targets -- -D warnings
