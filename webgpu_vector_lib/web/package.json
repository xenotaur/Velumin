{
  "name": "web",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "check:wasm": "cargo check --manifest-path ../Cargo.toml --target wasm32-unknown-unknown",
    "build:wasm": "wasm-pack build .. --target web",
    "build": "vite build",
    "dev": "vite",
    "baseline": "npm run check:wasm && npm run build:wasm && npm run build",
    "test:visual-smoke": "npm run build:wasm && npm run build",
    "test": "npm run baseline",
    "smoke": "node smoke.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "vite": "^6.2.2"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "pngjs": "^7.0.0"
  }
}
